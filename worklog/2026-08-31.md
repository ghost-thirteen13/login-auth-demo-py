# 2026-08-31 工作日志

## 交付收尾

- 补齐 requirements.txt / .env.example / .env / .gitignore / README。
- 清理代码里的备考注释（15 个 .py 文件）。
- push 到 GitHub：ghost-thirteen13/login-auth-demo-py。

## 失败与修复

- flaky 测试：test_decode_token_rejects_tampered_token 偶发失败，根因是 base64url 末位低 2 bit 是 padding 位，改「篡改签名段首字符」。
- systemd 反复写失败：网页终端粘贴长命令断行（heredoc/printf/echo 都断），改 nohup 后台跑。

## 部署上线

- 阿里云 ECS（Alibaba Cloud Linux 3）dnf 装 Python 3.11。
- git clone → venv → 装依赖 → 配 .env（DeepSeek key + 随机 JWT_SECRET）。
- nohup uvicorn 监听 0.0.0.0:8000。
- 公网打不开：安全组没放行 8000 → 阿里云安全组入方向放行后正常。
- 公网 URL + 测试账号 viewer/Viewer@2026。
