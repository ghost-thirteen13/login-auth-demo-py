# 2026-08-30 工作日志

## 后端开发

- 分层：config → db → store → auth/permissions/moderation → schemas → routers。
- db.py 建表 + seed admin；store.py 校验 + 增删改查（强制 role=user）。
- auth.py：bcrypt + JWT + 三层依赖（解析→回库读最新→角色/能力判定）。
- moderation.py：规则层 + LLM 两层流水线 + 降级放行。
- permissions.py：compute_capabilities 能力矩阵（可见≠可改）。

## 失败与修复

- 昵称「傻逼」漏判：validate_nickname 的规则层敏感词是 TODO，测试发现后复用 check_rule_layer 补上。
- granted/banned 传 "yes" 被宽松转 True：Pydantic 默认 bool 太宽松，改 StrictBool 对齐严格类型检查。
- 测试：5 个文件 73 例全绿。
