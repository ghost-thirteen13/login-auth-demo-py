# 登录授权 Demo —— 开发总结

## 一、实现方式与时间规划

**实现方式**：采用**教练模式 AI 辅助**——AI 负责讲原理、给实现路径、兜底 debug，具体代码由我逐模块亲手实现，确保关键逻辑都经过自己的理解和验证。全程 **TDD**：先定接口契约，再写实现，最后补测试。

**时间规划**（3 天）：

| 日期 | 阶段 |
|------|------|
| 08-29 | 接题、需求拆解 |
| 08-30 | 技术选型 + 架构设计 + 开始开发 |
| 08-31 | 开发完成、73 个测试全绿、部署上线 |

前端界面复用了开源项目 `zar130530/login-auth-demo` 的现成实现，后端逻辑参考其设计思路后自行用 Python 实现。

## 二、整体架构与技术栈

**技术栈**：FastAPI + SQLite（标准库 sqlite3）+ bcrypt + PyJWT + httpx + DeepSeek（OpenAI 兼容接口）。

**架构分层**（职责单向，不反向依赖）：

```
config → db → store → auth/permissions/moderation → schemas → routers → public
```

**设计出发点**（三条安全铁律）：
1. **LLM 只审核、不碰权限**——审核结果只决定「能否注册」，角色永远由服务端强制为 `user`，审核与授权是两条硬隔离的链路。
2. **可见 ≠ 可改**——被授权访问资源 B 的用户只能「看」，管理能力（授权/封禁/删除）恒为 False。
3. **权限判定回库读最新**——每次请求回数据库取最新记录，撤销/封禁/删除立即生效，不信任 JWT 里的旧快照。

## 三、AI 辅助开发情况

- **工具**：Claude Code（CLI），通过 cc-switch 将底层模型切换为 DeepSeek
- **token 量**：约1300万 token
- **成本**：约 22 元
- **分工**：AI 主要承担三块——**后端骨架生成、测试脚本实现、部署纠错**；核心业务逻辑（权限模型、审核流水线、安全边界）由我亲自实现与验证，其余部分 token 消耗较少。

## 四、个人时间投入（不算 AI）

投入时间最多的是两部分：
1. **构建、理解与复现后端核心逻辑**——包括登录流程的鲁棒性、错误检测与边界处理；
2. **部署上线中的问题排查**——服务器环境调整、启动与公网访问的问题定位。

## 五、优先级判断

该场景下优先级最高的是**权限模型的正确性**，其次才是 LLM 审核。

理由：题目对 LLM 违规判断「不对最终效果做严格要求」，但资源 A/B 的授权是**硬指标**——授权逻辑一旦做错（普通用户能碰到资源 B、或管理能力泄露给非 admin），整个 demo 就废了。所以我把最多精力放在 **RBAC 能力矩阵 + 三层鉴权 + 纵深防御**上，把它做死；LLM 审核在保证「不碰权限」的前提下做到「规则层 + LLM 两层流水线 + 降级放行」。

当然，LLM 审核仍有明确局限——比如**谐音绕过**：`泥石竹`（谐音「你是猪」）这类隐晦变体，规则层和语义层都覆盖不到，会被直接放行。这是当前已知的边界，也是后续接入更细粒度检测或更强模型时可优化的方向。
